# 当前 Issue 开发记录

## 2026-09-09：Issue 收尾口径

维护者确认 Alpha/MVP 关单标准：功能满足当前范围、相关测试及必需 CI 通过、
没有已知阻塞问题，即可按实现交付收尾；不为所有已实现事项默认追加独立实机验收。
本节取代下方历史记录中“本记录不关闭 Issue”等当时的收尾决定，历史验证事实不变。

- #303、#326：开发要求已完成，补关；#305、#359、#374 中的相关实现已合并，必需 CI 通过。
- #340：恢复清单、导出/恢复工具与手册按 MVP 收尾；真实多节点恢复专项演练明确跳过，不冒称已通过。
- #352：目标选择与取消周期隔离按 MVP 收尾；真实长连接专项演练明确跳过，不冒称已通过。
- #6：继续暂缓，不在本次收尾范围内。

以下 8 项也已按该标准关闭，恢复原功能/修复标题，不再作为独立验收待办：

| Issue | 已交付实现 |
| --- | --- |
| #160 | 自建中继、隐私设置及运行时漂移对账 |
| #182 | 受管固定端点、Headscale 配置更新、来源校验及失效端点撤回 |
| #197 | 助手提案、可信审批、任务/SSE 结果回传及系统凭据隔离 |
| #198 | 当前 REALITY 精确 SNI、私有后端、固定回落目标及目标准入边界 |
| #204 | 按路由启用 Proxy Protocol v2、配置回读及切换保护 |
| #353 | 原生 Dante、多落地选择、Xray 路由、期望/已应用区分及故障门禁 |
| #356 | Caddy 易失目录、晚到私网地址及受管默认 Docker 网络恢复 |
| #365 | Access 会话配置持久化、已有入口原地同步及失败重试 |

已重新核对 #305、#359、#366、#374 合并状态及必需 CI 门禁通过；只承认相关作业
实际执行的测试，Alpha 跳过作业不算已执行。此前额外实机验收清单保留在 Issue 编辑历史，
不再作为本阶段完成条件，不转为新的重复 Issue 继续阻塞。发现具体可复现故障再跟踪。

#198/#204 不再要求已淘汰的 21000 companion、同 ASN 硬限制或启动/六小时全量复检。
既有认证、隔离和故障阻断边界继续保留；关闭不表示所有生产场景已实测或安全绝无问题。
不得把未执行的验收标记为通过。本次只复核既有 CI 并整理 Issue 与 PR 模板，
没有新运行的本地测试、生产操作或新版本发布。

后续完成型 PR 为每个已完成 Issue 写独立的 `Closes #编号` 关联并合入默认分支；
部分完成使用 `Refs`，同步更新具体剩余项。已经合并但漏关的 Issue 在核对后手动补关。

## 2026-09-09：最近 Issue 按编号复查

基线为 `main@52b331a`（`v0.1.0-alpha.114`），本轮工作分支
`kuddy/reconcile-recent-issues`。下表区分基线已有实现与本轮新增内容；
不能把历史 CI 的成功套用到本轮尚未提交的修改。

| 顺序 | 代码核对结果 | 本轮处理 |
| --- | --- | --- |
| #303 | `AppsView.tsx` 已分开说明普通应用的宿主机 443 冲突与受管 REALITY 的容器内部 443；中英文和回归用例均在基线 | 不重复实现 |
| #326 | `tailscalehost` 已分离安装固定版和安全下限，共享能力检查用于 Agent 安装、启动与接管 | Alpha CI 明确运行该包回归；不等待未来版本，不增加逐版本矩阵 |
| #340 | `recovery`、Center/Agent CLI、只读清单、应用卷证据与恢复手册已在基线 | 保留原身份、加密及前向迁移约束；按用户决定跳过真实多节点恢复演练 |
| #352 | 健康入口不再在启动/六小时周期隔离；节点执行有限候选检查并固定所选 IP，相同 HAProxy 配置复用 | 保留当前实现；按用户决定跳过长连接专项演练，不承诺目标永久安全 |
| #353 | 原生节点本地 Xray 路由、Dante、路由检查点和门禁已在基线；缺少期望/已应用出口区分与切换前提示 | 增加已应用出口持久记录、前向迁移 68、失败/陈旧回执保护、切换及重试确认、恢复原规则说明；补充固定 Catalog 镜像真实 Xray 配置 CI |
| #356 | Caddy 易失目录准备、默认 Docker 网络修复、原私网地址等待、身份绑定的网络资料恢复及业务/心跳隔离已在基线 | 保留现有修复和定向回归；未执行真实主机冷启动验收 |
| #365 | 默认 24h、可选会话时长、已存在 Access 应用原地更新、策略覆盖提示和部分失败重试已在基线 | 不重复实现；未更改任何线上 Cloudflare 配置或执行会话过期验收 |

本轮新增回归源代码覆盖：成功应用、失败切换、第一次启用失败、过期任务回执、
恢复原出口、历史迁移的已知/未知记录、取消不写、确认期间配置变化，以及原生
Xray 对原始/启用/切换/恢复配置的解析。Xray CI 使用 Catalog 摘要固定的 3x-ui
镜像、禁网只读容器与 `run -test`，并含非法协议负例；不是外网 TCP/UDP 出口、
DNS、生产重启或反中转抓包的替代验收。

按仓库要求，本轮只编辑并人工审查代码、格式化 Go 和生成 OpenAPI；
未运行本地测试、构建、lint、类型检查或浏览器 QA，未提交、推送或修改线上。
新改动须经后续正常 PR CI；本记录不关闭 Issue，也不宣称未执行的验收已通过。

## 2026-09-08 历史记录

2026-09-08，工作分支 `kuddy/complete-open-issues`。这是开发记录，
不是 Issue 关闭证明、测试报告或发布公告。

| Issue | 本轮代码 | 尚未完成 |
| --- | --- | --- |
| #356 | 主机重启后 Caddy socket 目录恢复；保留受管容器并修复默认网络端点；自定义别名、静态地址、驱动参数、Links 或网关优先级保持原状，不擅自重接；等待原私网地址；心跳与业务恢复隔离；按原节点身份恢复网络资料；页面显示简洁恢复状态 | 定向 CI 已覆盖；真实重启验收未执行；不把在线心跳等同服务健康 |
| #352 | 取消启动/周期隔离；保留相同 HAProxy 容器；实际 VLESS 节点检查有限候选池；显式选择并绑定成功检查的 IP；修正监听恢复误清 DNS 错误、历史迁移丢失安全隔离 | 定向 CI 已覆盖；实际节点候选可用性和长连接不中断验收未执行 |
| #340 | 组件恢复清单、只读 API/CLI、Agent/内置 Headscale 加密导出和新目录恢复、外部卷证据、前向迁移与恢复手册；CI 从历史 schema 升级并比较新旧结构 | 加密归档与数据库恢复 CI 已覆盖；真实多节点恢复演练未执行；外部卷恢复结果是管理员声明，不是异地备份证明 |
| #353 | 原生 Dante 无密码私网来源授权；双架构嵌入式产物；安装/停用和开机防火墙前置；Center 任务、Agent 路由检查点与故障阻断；精确 Headscale 来源规则；页面选择、节点开关与健康上报 | 首版代码已提交；`be11bb0` CI 通过真实 Dante 收发、systemd 生命周期及 Center 开关顺序；未部署生产或执行逐平台运行矩阵 |
| #326 | 默认安装版本与最低安全/兼容版本独立；达到下限的稳定版通过必要能力检查即可复用；当前下限依据 TS-2026-011；按用户要求删除等待未来版本和逐版本集成验收条件 | 不再受未发布版本阻塞；随本 PR 审查和正常 CI，不宣称验证所有未来二进制 |

#326 安全下限依据：[TS-2026-011](https://tailscale.com/security-bulletins#ts-2026-011)，
官方修复版本为 `1.102.3`。最新版与安全下限概念独立，不再等待更高版本发布。

本轮仅编辑代码、文档、回归用例并进行格式化和 OpenAPI 文档生成；按用户要求，
没有运行本地测试、构建、lint、类型检查、浏览器 QA 或线上实验。
本批改动已推送至[草稿 PR #359](https://github.com/petauron/vastora/pull/359)；之前的登录信息隐藏改动一并保留。
已补充 Alpha CI 的恢复与 REALITY 定向检查。CI 实际发现并推动修正了订阅回读夹具、
网络候选缺失字段、历史迁移夹具、DNS 错误状态以及 nftables 规则回读。
新增前向迁移 64，修复旧迁移把不安全的 REALITY 入口改回 pending 的问题，
同时从已保存的监听任务中撤回相应路由；已发布的迁移文件没有改动。

提交 `8e5c112` 的 [CI](https://github.com/petauron/vastora/actions/runs/34182739205)
已通过定向 Go 回归、前端、部署、安全检查、真实 Linux nftables 过期和原生防火墙回读检查，
以及固定版本 Xray 配置检查。新增套接字检查验证专用进程限制，不冒充真实落地部署验收。
后续提交仍须以各自 CI 为准。Alpha 未执行的全量 race、跨架构构建、真实灾难恢复和故障演练
不能计为已通过。

用户已授权完成后 review、合并，但没有授权本轮发布或修改生产环境。
按用户决定，Alpha 阶段跳过真实多节点恢复和长连接不中断两项专项演练；这两项不作为合并阻碍。
首版代码与 CI 证据已提交；不关闭上述 Issue，不把 CI 视为生产验收，
不能将本记录用于自动批准发布或关闭全部 Issue。
