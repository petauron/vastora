# 当前 Issue 开发记录

2026-09-08，工作分支 `kuddy/complete-open-issues`。这是开发记录，
不是 Issue 关闭证明、测试报告或发布公告。

| Issue | 本轮代码 | 尚未完成 |
| --- | --- | --- |
| #356 | 主机重启后 Caddy socket 目录恢复；保留受管容器并修复网络；等待原私网地址；心跳与业务恢复隔离；按原节点身份恢复网络资料；页面显示简洁恢复状态 | CI 和真实重启验收未执行；不把在线心跳等同服务健康 |
| #352 | 取消启动/周期隔离；保留相同 HAProxy 容器；实际 VLESS 节点检查有限候选池；显式选择并绑定成功检查的 IP | CI、实际节点候选可用性和不中断验收未执行 |
| #340 | 组件恢复清单、只读 API/CLI、Agent/内置 Headscale 加密导出和新目录恢复、外部卷证据、前向迁移与恢复手册 | 真实多节点恢复演练未执行；外部卷恢复结果是管理员声明；不是异地备份证明 |
| #353 | 直连/DERP/peer-relay 判定；有时限的 nftables 桥接业务阻断与检测循环；身份、版本、TCP/UDP 证明和规则回读约束；回归用例 | 原生 Xray、开机阻断依赖与 Agent 接线、真实 TCP/UDP/DNS/IPv6 路径、任务与页面开关、切换回滚和验收仍未完成；不可交付为落地机功能 |
| #326 | 保留现有稳定版本兼容策略；再次查询上游稳定版 | 当前上游 latest 仍为 `v1.102.3`，没有比兼容下限更高的真实稳定版可完成验收；不以合成版本或预览版冒充 |

#326 上游证据：[Tailscale 最新 Release](https://github.com/tailscale/tailscale/releases/latest)，
2026-09-08 查询返回 `v1.102.3`，发布时间 2026-08-20。这个外部状态可能变化。

本轮仅编辑代码、文档、回归用例并进行格式化和 OpenAPI 文档生成；按用户要求，
没有运行本地测试、构建、lint、类型检查、浏览器 QA 或线上实验。
本批改动已推送至[草稿 PR #359](https://github.com/petauron/vastora/pull/359)；之前的登录信息隐藏改动一并保留。
首次 CI 中 Go 静态检查、更新恢复定向用例、部署检查与安全扫描通过；前端一条旧文案断言失败，
已按新的显式选站流程修正，待后续 CI。Alpha CI 未执行全部 Go 回归用例，不能等同全部验收通过。
草稿 PR 不代表功能已验收，不关闭上述 Issue；合并、发布及线上修改不在本轮范围。
后续优先完成 #353 的端到端业务链路，再对上述验收逐项取证，不能将本记录用于
自动关闭全部 Issue。
