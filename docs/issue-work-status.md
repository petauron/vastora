# 当前 Issue 开发记录

2026-09-08，工作分支 `kuddy/complete-open-issues`。这是开发记录，
不是 Issue 关闭证明、测试报告或发布公告。

| Issue | 本轮代码 | 尚未完成 |
| --- | --- | --- |
| #356 | 主机重启后 Caddy socket 目录恢复；保留受管容器并修复网络；保留网络别名、驱动参数与网关优先级；等待原私网地址；心跳与业务恢复隔离；按原节点身份恢复网络资料；页面显示简洁恢复状态 | 定向 CI 已覆盖；真实重启验收未执行；不把在线心跳等同服务健康 |
| #352 | 取消启动/周期隔离；保留相同 HAProxy 容器；实际 VLESS 节点检查有限候选池；显式选择并绑定成功检查的 IP；修正监听恢复误清 DNS 错误、历史迁移丢失安全隔离 | 定向 CI 已覆盖；实际节点候选可用性和长连接不中断验收未执行 |
| #340 | 组件恢复清单、只读 API/CLI、Agent/内置 Headscale 加密导出和新目录恢复、外部卷证据、前向迁移与恢复手册；CI 从历史 schema 升级并比较新旧结构 | 加密归档与数据库恢复 CI 已覆盖；真实多节点恢复演练未执行；外部卷恢复结果是管理员声明，不是异地备份证明 |
| #353 | 直连/DERP/peer-relay 判定；有时限的 nftables 桥接业务阻断与检测循环；严格认证的 HTTPS 出口和 UDP DNS 数据检查；原生 Xray 26.3.27 配置与受限 systemd 单元生成；隔离 Linux 内核验证放行过期和重启关闭 | 原生服务安装、源地址/出站防火墙、开机阻断依赖、Agent 任务与页面开关、切换回滚、真实 TCP/UDP/DNS/IPv6 故障验收仍未完成；不可交付为落地机功能 |
| #326 | 保留现有稳定版本兼容策略；再次查询上游稳定版 | 当前上游 latest 仍为 `v1.102.3`，没有比兼容下限更高的真实稳定版可完成验收；不以合成版本或预览版冒充 |

#326 上游证据：[Tailscale 最新 Release](https://github.com/tailscale/tailscale/releases/latest)，
2026-09-08 查询返回 `v1.102.3`，发布时间 2026-08-20。这个外部状态可能变化。

本轮仅编辑代码、文档、回归用例并进行格式化和 OpenAPI 文档生成；按用户要求，
没有运行本地测试、构建、lint、类型检查、浏览器 QA 或线上实验。
本批改动已推送至[草稿 PR #359](https://github.com/petauron/vastora/pull/359)；之前的登录信息隐藏改动一并保留。
已补充 Alpha CI 的恢复与 REALITY 定向检查。CI 实际发现并推动修正了订阅回读夹具、
网络候选缺失字段、历史迁移夹具、DNS 错误状态以及 nftables 规则回读。
新增前向迁移 64，修复旧迁移把不安全的 REALITY 入口改回 pending 的问题，
同时从已保存的监听任务中撤回相应路由；已发布的迁移文件没有改动。

提交 `0ed4856` 的 [CI](https://github.com/petauron/vastora/actions/runs/34181441607)
已通过定向 Go 回归、前端、部署、安全检查和真实 Linux nftables 过期检查。
后续提交仍须以各自 CI 为准。Alpha 未执行的全量 race、跨架构构建、真实灾难恢复和故障演练
不能计为已通过。

用户已授权完成后 review、合并，但没有授权本轮发布或修改生产环境。
草稿 PR 不代表功能已验收；#353 的端到端业务链路仍未完成，不关闭上述 Issue，
不能将本记录用于自动批准合并或关闭全部 Issue。
