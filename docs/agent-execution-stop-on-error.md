# #412 执行策略改造进度

状态：已接通新授权的任务领取、Agent 主循环与结果业务投影，尚未完成 #412，也未部署。旧证据迁移、人工处置、执行器内部的变更步骤/补偿和 helper 交接仍需完成，不能当作完整停止策略发布。

## 协议切换暂停与控制审计

74→75 前向迁移在同一迁移事务中设置领取暂停，并记录 migration:75 审计事件。暂停跨重启保留；管理员明确恢复后，重启不会重新暂停。新安装无旧协议切换，不设置迁移暂停。管理员每次暂停/恢复均追加操作者、时间和目标状态，审计失败时控制状态一起回滚。迁移前备份、失败无部分状态、升级暂停、重启保持、显式恢复及新安装行为已通过针对性测试。

暂停只阻止新任务领取，不取消已经签发的执行；还需要管理界面和旧执行/证据核对流程，不能仅凭解除暂停就认为协调切换已完成。

## 管理界面接入

设置页已接入真实执行列表和游标分页、暂停/恢复领取，以及后端已支持的放弃、重新执行和助手确认完成。列表使用简洁状态文案；阶段与错误保留在详情中，旧回执只读显示不含凭据的摘要。人工处置要求明确确认旧执行停止并填写核对说明；读取离开页面时取消，提交失败不自动重试。尚未支持的旧回执处置和普通任务确认完成不显示可操作按钮，这两项仍须完成。

8 个针对性模型/API/React 交互测试通过，类型检查和新增文件 lint 通过。使用已有 Playwright 在临时本地 Vite 页面进行隔离渲染验证（模拟 API，不操作真实节点）：1280×900 浅色和 375×812 深色、减少动画；恢复确认和重新执行确认交互通过，无页面错误或框架覆盖层。截图发现并修正小屏状态徽标覆盖时间的问题，复查实际 flex 布局及无水平溢出。此证据只证明组件交互，不代表真实 Center/Agent 完整端到端或整个设置页 QA 已通过；临时服务已停止。

## 结果投影与执行终态

application.command 也已接入同一确认入口：复用原有子命令结果校验与事务提交，保留原 attempt；应用已有更晚命令时拒绝覆盖。实际订阅配置命令回归使用已保存结果、Agent 进程更替及管理员确认完成投影；审计 UPDATE 故障时命令仍 running、result_json 未写入、disposition 为空，移除测试故障后显式确认才成功，原执行仍 unknown。该订阅流程及应用确认通过两轮 race。接口支持不代表每种子命令关联资源的故障窗口均已覆盖；旧回执、凭据核对及管理界面仍待完成。

确认接口已进一步覆盖 landing.server/proxy、gateway.routes/component、node.listener 和 tunnel 六种运行时任务。正常回报和人工确认共享各自投影函数及事务提交回调，处置记录与业务结果同时提交；仍须同一任务 ID、revision 和 attempt。未实际提交的过期投影不能被当作确认成功。没有保留成功证据或证据为未知时不允许确认，不能凭节点在线推断完成。

六类运行时在真实 schema/执行授权证据上的 24 个定向场景通过：成功、延迟外键 COMMIT 失败、新 revision 替代以及 applied revision 已达到目标。失败保留原状态及加密结果，成功保留历史 unknown/attempt 并记录 disposition，重复确认拒绝。运行时确认、应用确认和原子结果提交共同通过两轮 race。运行时夹具没有真实公网入口，落地使用停用计划；此证据不代表所有关联资源/开启落地身份校验的端到端覆盖。应用命令、旧回执、页面入口及后续资源协调仍待收尾。

应用部署现增加 POST /api/v1/executions/{executionID}/confirm-completed：管理员在核实旧执行停止后，使用已保留的成功结果及原始运行时版本确认。普通 Agent 结果入口与人工确认复用 projectApplicationDeployment 校验/投影；人工确认与 disposition 同事务提交，历史 unknown/failed 状态、加密结果及 attempt 不改写，不签发新授权、不调用 Agent 执行器。更新后的业务行必须仍是该应用最后一个部署及同一 attempt。当前入口仅支持 application.apply，其他普通任务及旧回执处置、页面入口、凭据轮换和后续资源协调仍需完成；不应将该入口当作完整人工处置功能。

针对性验证覆盖成功、审计更新失败、延迟外键提交失败、无效结果、缺失版本、失败结果、未知结果、非管理员及未确认停止；失败不留下部分服务/业务投影，原始证据不变。真实 HTTP 覆盖匿名、缺 CSRF、管理员及重复确认；新接口契约和人工确认测试通过两轮 race。此前全部 TestExecution 通过两轮 race；本次不代表完整包回归或生产验证。

加密结果证据现保留原始 applicationRuntimeGeneration（可空），不从后续心跳或当前任务配置推断。HTTP 入口将收到的可空值原样交给证据存储；缺失仍为 null，而不是隐式零值。六种真实 HTTP 结果提交场景检查了原始结果、成功/未知标记和版本证据；缺失版本时业务投影拒绝，原始证据与执行屏障保留。全部 TestExecution 定向测试通过，包含缺失版本反例的提交故障窗口通过两轮 race。此项只是人工确认所需的证据补齐，普通任务确认完成和旧回执处置仍未完成。

生产结果入口已将业务投影和执行终态合并提交：各任务族及应用子命令在自己的业务事务内调用统一收尾回调，核对业务状态与上报结果一致后，更新执行终态并提交。没有实际提交的过期/重复投影不得返回完成。原始加密结果仍先独立保存，投影失败保留 result_received 屏障供人工核对，不重放；只有证据留存不代表任务完成。独立最终提交及无授权 CompleteTask 入口仅作为测试夹具保留，不在生产中提供。

应用结果校验、服务/凭据写入或关联任务排队失败时直接回滚，不再把局部失败改写成另一种业务结果后继续提交。生产入口核对所有任务族投影后的状态，防止 Agent 上报成功而子命令实际拒绝结果时错误标记成功。结果冲突返回 409，不误报为鉴权失败。

真实 HTTP 结果路径覆盖正常提交、最终状态 UPDATE 失败、延迟外键 COMMIT 失败、服务 INSERT 失败和无效应用结果：失败时业务状态/服务无部分变更、无 reported 审计、加密结果保留、领取仍被阻止、重复结果拒绝。实际 Agent→Center 流程及领取故障窗口通过两轮 race，全部 TestExecution 与落地流水线定向回归通过。随后还补齐嵌套落地客户端命令的提交回调。

仍须审计部分投影提交后的发布清理、自动收敛等外部副作用，并为所有任务族补完整故障窗口；本节不代表全部执行边界、旧证据人工处置或长期资源验证已完成。

## 失败后的订阅入口与后台转换

订阅配置失败只记录失败，不再自动调用 StopPublication。创建订阅命令时，随机值、事务或持久化失败也不撤销已创建/复用的入口；已有入口的停止属于新的显式操作。回归测试覆盖运行中的订阅配置失败和后续命令 INSERT 故障，入口状态、期望/应用版本及更新时间均不变。原订阅回归夹具已补上现行 API 必需的入口节点参数，不增加兼容回退。

失败或要求核对的应用结果不再触发控制主机自动转换。自动转换入口本身也检查相关 3x-ui 执行屏障：offered/running/helper_running/failed/unknown 未处置执行均在读取转换计划前返回。测试用不可读取的计划视图作哨兵，每种状态连续三轮均未触发计划读取；显式去除屏障后哨兵可见，证明检查没有被其他早退分支掩盖。针对性回归通过，其他后台和外部副作用仍需逐项审计。

## Agent 更新链路

- 已领取但尚未完成安装交接的更新，租约过期后保留原 attempt，记为失败、需要人工核对，不自动重新排队。
- 安装交接要求有效租约且只消费一次；失败、已完成或已经进入 installing 的同一 attempt 都不能再次取得安装许可。已保存结果走独立回报流程，不重新请求安装交接。
- 自动批量更新跳过最近一次更新失败的节点，包括后续版本；进度显示需要处理，不显示无限等待。其他节点仍独立更新。
- 新调度的更新 helper 只启动一次，不配置失败重启，也不注册开机重跑。安装记录、凭据保护、候选程序和数据库备份不因此删除；成功后的受保护清理仍保留。
- 现有线上 helper 不会被本地代码修改替换，旧失败节点仍需单独核对。中途失败可能要求人工恢复管理进程；此改动不保证自动恢复连接。
- 更新助手不再在联系 Center 前离线激活恢复点；已保存的结果只回报，不重新启动服务。发现已有恢复点就停止并要求显式维护，不再自动补装候选程序。停止 Agent 后的备份/安装失败也不自动重启旧版本，原可执行文件、候选程序和数据库恢复材料保留。更新助手相关测试通过两轮 race 检查，含启动失败后的再次调用、准备恢复点失败、断点保留、Center 不可达及成功结果回报失败。新的单次执行授权交接尚未实现，因此这不是可发布的完整更新链路。
- 一次性 begin 交接已与结果回报分离，丢失 begin 响应不再通过重复请求重新激活助手。本轮更大范围本地命令测试匹配到 Linux 专用下载测试，在 macOS 因平台限制提前失败并触发测试同步错误，不能视为通过；随后将 Center 更新任务与 HostUpdate 生命周期测试分开执行。新 execution ID/进程会话与 helper 身份的完整绑定仍待接通，不把旧任务 attempt 的单次消费当成完整新协议。
- 已增加 Center 新授权交接事务：仅当前会话的 agent.update、running/handoff 阶段且两套租约均有效时转为 helper_running，同时更新业务状态为 installing。重复消费被拒绝；原 Agent 不再能推进该授权；Agent 进程更替保留助手运行屏障。租约冲突时事务回滚、不消费授权。相关测试和 74→75 迁移测试通过两轮 race 检查。该新事务尚未接入 HTTP/Agent/助手请求，助手超时及结果终结也待绑定；禁止据此宣称新更新链路已完成。
- 上述事务现已接入 HTTP、Agent handoff 阶段及助手操作文件，开始/结果请求携带 executionId/sessionId。助手状态单独保留，结果仍绑定原任务/attempt/执行/会话且受处置与过期约束；Agent 换进程不会夺走正在运行助手的结果回报权。助手授权限时 30 分钟，领取检查发现过期后转 unknown，不自动续跑。三包 Execution/HostUpdate 定向回归通过；真实 HTTP 开始交接、Agent 会话更替后成功结果入账、重复结果拒绝通过两轮 race 检查。尚需补充助手每个后续变更步骤的在线授权检查、失败/超时/人工处置的完整交互以及 Linux 完整更新验证，不能据此发布。
- 助手现在在 stop/backup/preserve/install/start 五个阶段分别请求 Center 单次授权，严格按前一阶段推进，不延长总授权期限；跳步和重复消费均拒绝。实际激活代码每阶段之前执行检查，失败不启动后续变更。真实 HTTP 顺序测试、HostUpdate 回归及五个阶段授权失败的文件/服务操作断言均通过两轮 race 检查。阶段内部的系统调用仍可能先于错误生效，材料保留且按未知结果报告；此验证不包含真实 systemd/完整 Linux 更新，也不代表其余安装/卸载执行器已完成同等审计。
- 助手成功结果必须已到达授权 start 阶段，提前报告成功会拒绝。过期助手结果被拒绝；重新检查领取时执行变为 unknown，保持阻断，迟到结果与步骤请求均不能解除。此故障窗口与正常 Agent/Center 流程通过两轮 race 检查。仍发现旧 agent_updates 业务投影会把 recoveryRequired 留在 installing，且手动 QueueAgentUpdate 需统一接入人工处置，尚未修正这一显示/调度边界。
- 上述业务投影边界已修正：recoveryRequired 的更新记录为 failed，不再保持 installing；普通 QueueAgentUpdate 不会在最近一次失败后创建新更新。版本心跳即使达到目标版本，也不自动将失败改成成功，汇总保留 Failed 而非 Updated/Updating。已有失败结果的重复内部投影保持幂等，但新执行协议的 HTTP 层仍拒绝重复结果。显式人工处置的完整入口仍待实现，不能借普通更新按钮绕过。
- 更新任务已增加管理员确认完成/放弃 API（resolve-update），要求旧执行已停止及核对说明；确认完成还要求近期目标版本观察。业务状态与处置审计在同一事务提交，原 failed/unknown 执行证据不改写。放弃后的自动 rollout 不重新生成更新，后续显式 QueueAgentUpdate 可创建新意图；迟到旧结果与重复处置被拒绝。HTTP 成功路径、非管理员拒绝、缺少停止确认、过期观察拒绝及原始证据保留测试通过两轮 race 检查。尚未提供管理 UI，其他任务类型的确认/放弃、资源冲突验证和旧助手材料的后续维护仍待完成。

这部分复用现有任务状态。更新链路定向回归测试已通过，未修改线上节点。

## Center 执行授权底层

- 增加单次执行授权、进程会话边界、阶段确认和持久审计。授权响应丢失不会让同一授权再次启动；进程变化或授权超时保留未知结果屏障。
- 任务快照与结果证据在 Center 加密保存，列表只返回公共诊断字段。结果证据写入不立即解除屏障，业务状态提交后才允许完成授权。
- 新增 74→75 前向迁移，沿用既有迁移前备份和失败关闭机制，不支持数据库降级。
- 已运行并通过：授权/启动响应丢失、进程更替、结果响应中断、终态不可重放、失败保持屏障、加密证据、协议不匹配、存储不可用，以及迁移备份、失败不推进版本、新建/升级结构一致性的定向测试。
- 增加会话注册和执行 start/step/stop HTTP 接口，每次执行变更验证 Agent 凭据。停止保留最后确认阶段，失败或未知结果均保持屏障；迟到停止不能覆盖已保存结果。
- Center 保存进程会话历史，退役会话不能通过重新注册恢复权限；当前会话重复注册不重新授权。历史不下发到 Agent。
- 已通过真实 HTTP handler 的凭据拒绝、会话隔离、启动不可重放、停止后阶段不可推进、审计及退役会话拒绝测试；新增表的新建/迁移一致性测试通过。
- 任务领取接口要求已注册的进程会话，先保存授权和加密快照再交付；响应丢失、访问同步失败或加密失败不释放已有授权。Agent 解密后核对授权协议和内容摘要，成功消费启动授权后才进入执行器。
- Agent 主循环不再轮询本地待上报任务或自动恢复/重放，也不运行原有周期维护。新执行只做一次结果回报；Center 先加密保存结果，再提交业务投影，最后终结授权。CLI 首次心跳失败也不再触发离线恢复。
- 当前旧回执仍作为切换前的阻塞证据保留：存在未确认记录就拒绝进入新执行循环，尚未实现转移/人工处置与最终删表。不能因此宣称旧回执迁移已完成。
- 已通过真实 Agent RunTasks → 加密任务领取 → Center 单次授权 → 模拟 Docker 执行器 → 业务结果提交的成功/失败集成测试。结果回报丢失后重启 Agent，原执行变为 unknown，不再次执行。外部 Docker 副作用为计数模拟，不是生产运行验证。
- 授权响应丢失、启动确认响应丢失也已通过真实 Agent 循环测试：执行器调用次数为零，重启后原授权保持 unknown 屏障。五种执行场景和 HTTP 授权测试通过两轮 race 检查。
- 已增加鉴权执行列表与显式重新执行 API：要求管理员确认旧执行已停止并记录核对说明；原授权处置审计与任务重新排队在同一事务提交。下一次领取必须产生新 attempt 和新授权，不改写旧执行结果。旧 reconciliation 重试入口拒绝绕过这套处置。
- 重执行支持各任务类型的精确 attempt/修订号条件更新，并校验 Center 加密任务证据。应用部署有真实 Agent 循环验证；其他类型目前只有真实 schema 上的语句检查，仍需补充实际处置验证。确认已完成/明确放弃、跨资源冲突检查和 UI 尚未补齐。更新和卸载 helper 的新授权交接也尚未接通，禁止据此发布。
- 授权续期独立于阶段更新，不再将已进入 persist/report 的最后阶段误写回 apply；结束或失效的执行不可续期。
- 底层租约回收不再将部署、命令或卸载任务重新排为 pending；保留原 attempt，记录需要核对，并将已有授权置为 unknown。网关、监听器、落地、隧道和卸载领取器不再自动领取 failed 状态。Store 领取入口也检查授权屏障，避免内部调用绕过 HTTP 检查。
- 已更新并通过租约到期、续期失效和 Agent 启动后的停止测试：不产生新 attempt，迟到部署结果被拒绝，原核对信息仍保留。测试不代表所有 desired-state 自动生成路径均已完成审计。
- 心跳对运行中/失败/未知执行保持观测，但不自动排入运行时迁移、网关修复、监听器重配、落地来源协调或定时备份；会触发发布清理的端点协调也暂停。已验证连续心跳在这些执行状态下不产生新运行时部署，版本观测继续更新；无屏障的原有调度回归通过。其他后台入口及跨节点依赖仍需继续覆盖。
- Store 以进程内所有者管理当前执行取消，不写任务历史；执行期间的控制 HTTP 失败与心跳失败会取消当前 context，Store 退出也会取消。迟到的旧执行请求错误和旧清理回调不能取消/清除新的执行所有者。相关针对性测试通过两轮 race 检查。执行器内部忽略取消或使用 WithoutCancel 的后续变更路径仍需逐项移除，不能仅凭 context 取消宣称所有外部步骤已停止。

## 隔离 Linux 验证环境

本机没有运行中的 Docker daemon。用户随后明确指定了非生产 Linux 测试机。已在独立的 256 MiB/512 MiB 无 Swap 容器中运行心跳身份读取、执行取消与 HTTP/心跳失败测试，各通过 10 轮，无 OOM；另通过连接与 Monitor 生命周期测试。详细指标和证据边界见 `agent-local-http-lifecycle.md`。这是对应测试范围的验证，不是完整 Agent 在正常任务、Center 长期故障下的低内存运行证明。未改动或重启已有服务，测试机标识和地址不写入仓库。

网关 desired-state 应用现在在配置提交、健康验证或本地状态保存出错后保留待核对结果，不自动写回旧配置；在途取消后收到迟到的成功返回也不会继续保存新版本。针对性两轮 race 测试覆盖健康失败、取消、原有版本幂等逻辑。底层 Caddy/共享端口及其他执行器内部的补偿路径仍需继续审计。

进一步移除了 ManagedGatewayDriver 在共享 443 失败后的 Caddy/运行时回滚，以及节点监听器失败后的旧 HAProxy/网关配置回滚；同版本监听器健康检查失败不再隐式重应用。保留明确移除监听器的正常端口交接。监听器失败停止、首次切换失败、版本/所有权、正常移除与共享端口相关定向测试通过两轮 race 检查。心跳不再依赖旧启动恢复完成标记，改为使用实际只读健康观察，避免新循环不启动恢复后永久误报入口异常。

较大范围的网关/监听器回归曾检出旧启动恢复测试与新协议不一致（旧 RunTasks 期望离线恢复，以及直接调用恢复函数后期待更新旧启动标记）；这些尚未随旧恢复机制整体清除，不能据此声称整个 Agent 测试集通过。两处监听器测试使用无效证书占位值的问题已改用现有连接夹具并通过重验。

已删除 RecoverStartupRuntime 和 RestoreNodeListenerStartup 离线启动恢复入口；心跳不再发送 runtimeRecovery/runtimeRecoveryApplications 旧字段。真实 Agent/Center 集成测试显式监视执行器 Restore/Maintain 调用次数，在成功、失败、领取/启动/结果响应丢失及进程更替场景中保持零，两轮 race 检查通过。只读心跳及控制面失败取消的定向测试也通过两轮 race 检查。Center 旧恢复字段、筛选协议和底层恢复实现仍需随协议/迁移收尾清理；不能把删除启动入口等同于完成全部旧机制清理。

Agent 领取函数已移除旧恢复范围及指定历史任务 ID 参数。Center HTTP 领取入口只接受单个 wait 参数，明确拒绝 recovery、taskId（包括空值）和重复 wait，不静默降级到普通领取。相关鉴权/停止屏障测试及实际 Agent/Center 五种执行场景再次通过两轮 race 检查。Center 内部存储层旧筛选函数及历史字段仍待删除，不代表旧协议内部实现已完全移除。

Center 内部领取器随后也移除了 RecoveryScope 参数、恢复应用筛选和 claimRecoveryStopTask 分支。旧恢复模式领取测试被新单次授权/HTTP 拒绝测试替代；保留历史恢复状态存储测试，直至字段迁移完成。补充真实排队自更新的屏障断言：已有执行未确认时，更新保持 pending、attempt 为零，不能越过屏障。执行/过期租约/历史状态定向测试通过，授权故障窗口及 Agent/Center 流程通过两轮 race 检查。内部测试仍使用的指定任务 ID 选择器、历史恢复状态和 Agent 旧回执尚待后续清理。

## #412 尚未完成的必要项

1. 将应用部署、命令、网关、落地、卸载等执行路径统一改为失败停止，涵盖租约过期、启动恢复、周期维护和控制面断连；不能只修改更新 helper。
2. Center 持久授权、资源冲突隔离和阶段证据。人工确认完成、放弃或明确重新执行必须有审计；重新执行必须核对旧执行已停止并发出新授权。目前已有的更新排队入口不等于完整人工核对协议。
3. 替换 Agent 的持久回执、自动恢复及轮询逻辑；先完成 Center 协议和旧回执/一次性结果迁移，再删除旧机制，不能提前丢弃凭据或未知执行证据。
4. 更新助手已接入单次执行授权、原子阶段交接和更新任务人工处置；仍需补全其他任务类型的人工处置、资源冲突核查，并审计下载重试和安装/卸载助手。必须保持前向数据库迁移安全边界，不能简单删除恢复材料。
5. 协调协议切换、拒绝不匹配 Agent，以及故障窗口和小内存节点资源验证。

发布前需要验证这次调度变更；#412 的关闭还需要完成上述剩余项，不能把本片作为完整实现合并关闭。

## 升级观察与迁移停止补充验证

更新助手在提交成功结果前，有界只读等待 Center 观察到目标版本心跳。正常未就绪最多检查 30 次、总期限 30 秒；网络、鉴权和取消错误立即结束，不重复安装或启动。目标心跳、错误会话拒绝，以及就绪/持续未就绪/503/401/取消五种助手路径通过两轮 race 检查。

3x-ui 控制主机迁移已移除失败后自动回滚数据库、使用备用 Token 重复导入、依据本地阶段恢复执行的分支。保留受保护恢复材料，未知状态等待显式人工处置。针对 prepared/imported/api_ready/role_configured/applied 五种已有阶段的测试确认再次进入时零 HTTP 请求、阶段和原始备份不变；两轮 race 检查通过。完整人工迁移处置仍属于上列必要项。

本轮较大范围 `internal/agent`、`internal/landing`、`internal/center` 包回归中，landing 通过，Agent 和 Center 未全部通过。已发现旧启动/重放夹具、卸载助手授权接入、OpenAPI 缺少新执行路由，以及凭据、历史迁移和部分应用夹具等失败；尚未逐一归因与修复，不能以定向测试通过宣称完整回归通过。未操作生产环境。

后续已补齐执行会话、阶段转换、历史列表、重新执行和更新处置的 OpenAPI 路由与鉴权/请求约束，并修正生成器遗漏 `decodeJSON(r, ...)` 请求体的问题。针对协议版本、启动摘要、阶段名、人工停止确认及处置类型的契约测试和路由一致性测试通过两轮 race 检查；条件必填采用可被当前验证库验证的显式 oneOf 分支。此项文档缺口已解决，不代表其余包回归失败已解决。

卸载助手与独立 finalizer 已改为 Restart=no、不配置开机启用；generator 只恢复可供人工检查的 unit，不创建开机启动链接。调度只执行一次 start。Tailscale logout 和停止命令失败立即返回，不再忽略错误继续卸包或删除文件。临时目录内的错误注入、文件保留和 finalizer 测试通过，关键停止路径通过两轮 race；真实 systemd 集成测试仅调整为显式 start，本轮未运行。卸载助手的 Center 单次授权交接、失败上报、回调与人工处置仍需接通。

随后已接通卸载助手的单次交接及失败/完成回调：Agent 在 handoff 阶段传递 executionId/sessionId，受保护操作文件缺少这两个字段会拒绝运行。Center 同事务消费 running/handoff 授权并把未过期任务转为 cleaning/helper_running，重复交接拒绝。公开回调使用任务绑定 Token，原子写入加密结果证据、执行状态和业务状态；过期、已处置及重复回调不改变记录。清理失败只使用新的有界上下文报告一次错误，记为 unknown/failed，不继续清理或补偿。会话替换、租约冲突回滚、重复/过期/已处置回调、成功及清理失败场景通过定向 race 验证；助手相关测试通过。

卸载仍有必要剩余项：逐阶段远端授权（尤其私网退出前后的公共控制通道）、人工确认/放弃/重新执行与受保护材料处理、执行器所有子步骤的取消传播及真实 systemd 故障窗口验证。上述交接与回调完成不等于完整卸载协议完成。

主机卸载外层流程已补充命令前后和文件删除前的取消检查，底层命令迟到成功不再使流程继续。启动前、停止 Agent、运行时清理、注销 Tailscale、停止 tailscaled、卸包六个取消窗口确认后续命令计数为零且剩余文件保留，两轮 race 检查通过。运行时清理器内部的细粒度步骤和其他执行器仍须继续审计，不能把外层取消检查等同于全链路逐步授权。

卸载外层阶段现已使用公共任务回调 Token 向 Center 申请递增序号的单次许可，包括命令、运行时清理、文件删除和最后完成阶段。Center 在事务中校验当前任务、Token、执行状态、有效期及前一序号；不延长原有助手期限，拒绝跳步、重复及完成后的继续操作。成功回调必须已有 done 阶段，失败则保存最后阶段为未知。退出私网后继续使用任务原先下发的公共回调地址，不依赖已删除的 Agent 本地状态。存储边界、真实 HTTP 路由、错误凭据，以及拒绝命令/运行时/文件阶段后零后续变更的测试通过两轮 race。

这仍不是全部执行器的逐命令授权：PurgeManagedRuntime、取消旧助手等复合操作内部及 finalizer 收尾尚需完成审计；其余任务种类的人工处置和旧回执迁移也未完成。未执行生产操作。

运行时清理流水线已移除 errors.Join 后继续执行的逻辑。应用、宿主探针、隧道、网关、四层代理及网关卷九个清理步骤各自检查授权/取消，首错立即停止；九个位置的命令失败、授权失败、命令迟到取消及授权迟到取消测试通过两轮 race。Docker 客户端仍由流水线唯一所有者 defer Close，不因早退漏关。单个应用或 provisioner 内部的 API 调用尚须继续审计。

取消旧助手时，命令及文件变更也接入公共阶段授权，错误后不再使用状态查询兜底继续删除。每个授权边界逐一拒绝的测试确认调用在该边界结束、拒绝后零后续命令；取消及本地明确卸载回归通过，两轮 race 通过。底层批量恢复材料删除函数和 finalizer 收尾仍有审计剩余项。

助手人工处置现统一为 `/api/v1/executions/{executionID}/resolve-helper`，替换仅更新专用入口（不保留别名）。更新确认仍要求新版本在线观察；卸载确认使用管理员对实际清理的明确核对说明，不将离线当作卸载证明。确认/放弃在同一事务中保存审计、保留原 unknown/failed 历史、更新业务状态，并清除卸载回调 Token。管理员/CSRF/停止确认/重复处置以及处置后旧助手与回调拒绝测试通过两轮 race。

已允许管理员明确重新执行过期但业务仍处于 cleaning/installing 的助手任务。领取产生新 attempt，卸载回调 Token 随新 attempt 重新生成；原 attempt 和 Token 不能影响新任务。过期卸载助手重新领取测试通过两轮 race。仍须实现保护材料处置与新助手实际启动的端到端流程、其他任务类型的人工确认/放弃，以及管理界面。

旧回执迁移已新增只读逐条导出接口：按 task_id 游标读取一致快照，先检查加密长度再加载解密，限制单条结果大小；核对任务身份、attempt、摘要长度和时间戳。结果保留原始 JSON，避免新类型丢弃旧字段或生成凭据。导出不确认、不修剪、不重放任务。分页、重复读取稳定性、未知字段/凭据保留、损坏密文、身份不匹配、过大及缺失完成结果测试通过两轮 race。Center 接收持久化、确认后本地退役和旧机制删除尚未接通；RunTasks 的旧证据屏障仍保留，不能宣称迁移已完成。

Center 已新增受 Agent 凭据和当前执行会话保护的 legacy-receipts 接收端，使用统一字段验证与摘要编码。存档采用独立的稳定命名空间写入执行历史，加密保存完整原始回执；相同内容只有在验证已落库密文完整性后才重复确认，不同内容拒绝覆盖。processing/completed 等未确认记录作为 unknown 阻止新任务；原 acknowledged 仅保留历史已确认状态，不投影任何新业务效果。存在活跃授权时不得新建存档。迁移端点限定 3 MiB 请求、2 MiB 完成结果，鉴权先于大请求解析；普通 JSON 接口仍为原有 1 MiB 限制。

Center 处理/完成/已确认三类证据的凭据与会话拒绝、加密内容完整性、稳定去重、冲突拒绝、未知状态屏障、HTTP 确认不含凭据和损坏存档拒绝测试通过两轮 race。Agent 自动转交、Center 确认后的本地退役、历史证据人工处置以及旧机制移除仍未完成，不能解除切换屏障。

Agent RunTasks 现先注册新会话，再逐条转交旧证据，确认全部转交后才请求新任务。确认必须同时匹配 archived 标志、由 Agent/任务/attempt 派生的存档 ID 及内容摘要；本地退役事务重新读取并核对该条证据，记录变化则拒绝删除。失败直接结束本轮执行循环，不自动重发业务结果；进程再次启动只可去重确认存档。普通 POST 仍保留原请求上限，仅迁移使用较大有界上限。

真实 Agent→Center 集成测试覆盖 Center 已提交但 HTTP 回复丢失：首次保留本地原件且零领取；再次启动取得相同存档确认后退役原件，Center unknown 屏障拒绝领取，执行/恢复/维护调用均为零。错误回复、损坏 JSON、错误 ID/摘要、本地同时改变证据的测试也确认不退役；两轮 race 通过。旧表及旧回执实现尚未删除，历史证据人工处置和最终协议切换/迁移收尾仍未完成。

更新汇总不再把目标版本心跳当成仍在执行任务的完成证据。pending/running/installing 任务即使报出目标版本或更高版本，在线且进度有效时仍计为 updating，超时计为 failed，离线计为 offline；查询不修改任务状态或重新执行任务。三种状态、目标/更高版本、在线/离线及超时窗口的针对性测试通过，两轮 race 回归通过。只有没有活动更新任务且没有未处理失败时，版本匹配才可计入 updated。此修正不代替尚未完成的历史证据人工处置及整体协议验证。

管理员可通过单条 legacy-receipt 查看接口读取原任务 ID、种类、attempt、原状态、时间和完成证据是否存在。该接口不返回 completion 内容、生成凭据或密文；在一致事务内先检查密文大小，再验证解密、内容摘要与存档身份。执行列表仍不加载这些大字段。未登录访问拒绝，已登录响应 no-store，损坏/摘要不匹配/超大存档均拒绝；查看不解除屏障、处置或重放任务。隐私边界和 OpenAPI 契约测试通过两轮 race。人工处置本身及对应管理界面仍待完成。

非助手执行现有独立 abandon 入口，要求管理员、CSRF、明确停止确认和核对说明。同事务将原任务精确 attempt/revision 标记 failed、取消领取与旧回执调度标志，并记录处置者与说明；不删除历史结果或凭据、不回滚资源、不创建新任务。应用部署、应用命令及六类运行时任务的 SQL 已核对迁移后 schema；真实 Agent→Center 应用失败流程验证非管理员/缺少停止确认/缺少 CSRF/重复处置拒绝，放弃后无重新领取，迟到成功结果拒绝。针对性流程与契约通过两轮 race。其他任务族的完整故障窗口、后台协调器是否会生成替代意图、历史回执处置和管理界面仍需完成，不能以此宣称整体人工处置已完成。

运行时版本迁移已修正一个后台替代意图入口：只迁移仍运行且最近一次部署成功的应用，不再跳过最新失败部署并复制更早成功配置。网关组件只从已完整应用的 ready 状态迁移，依赖路由存在未完成版本时也不升级；监听器与隧道同样要求 ready 且版本已应用。应用失败后连续心跳不排入替代配置，网关/监听器/隧道 pending、applying、failed 状态在三轮版本变化后保持 revision、attempt 和状态不变；健康应用与网关仍正常迁移。上述针对性测试通过两轮 race。其他后台入口、跨节点依赖及人工确认完成仍待继续审计。

落地来源刷新已补目标节点执行屏障，不能用来源节点的心跳或结果处理权限改写另一台有未确认执行的落地节点。心跳协调跳过目标的 unresolved 执行或 failed 落地状态；非心跳的直接来源刷新遇到 unresolved 目标明确失败，让调用方事务回滚。真实落地授权流水线中，offered/running/helper_running/failed/unknown 五种执行状态及 failed 落地状态各进行三轮跨节点协调，目标 revision 不变；直接刷新拒绝未确认目标。落地完整流水线、单写者队列及心跳只读观察测试通过两轮 race。其他跨节点后台路径仍需继续审计，不能将此等同于全部资源冲突隔离完成。

执行历史接口改为有界游标分页，避免固定最近 100 条隐藏更早的未知执行。每页最多 100 条元数据，nextCursor=0 表示结束；before 使用插入行游标，不随状态更新时间变化，无需读取或解密大任务/结果字段。205 条记录在翻页间插入新记录仍完整遍历旧记录、无重复；最早 unknown 记录可达。非法、空、重复及损坏查询参数拒绝。分页、Agent→Center 执行、历史回执和 HTTP/OpenAPI 契约通过两轮 race。管理界面接入和人工处置其余路径仍待完成。

旧 beginAgentUpdate 实现已删除，不再保留仅凭业务租约把任务置为 installing 的交接函数。原更新业务测试改为注册协议会话、持久化授权、消费 start、进入 handoff 后调用现行单次交接；重复/过期交接按新协议拒绝。更新任务、离线汇总、未知结果和原子助手交接测试通过两轮 race，源码搜索无旧函数定义或调用。其他旧回执/恢复代码的最终退役仍未完成。

新增管理员执行领取控制接口，持久化 paused、操作者和修改时间。领取前及领取事务内、授权签发事务内均复查暂停值；损坏或非布尔控制值不默认为允许。暂停不消耗 pending 任务的 attempt，不停止心跳/会话注册，也不取消已经签发的授权；解除暂停只允许正常领取，不能处置未知执行或重放失败任务。权限、CSRF、必填布尔值、连续暂停领取、损坏控制关闭和恢复原 pending 任务测试通过；与执行及更新交接回归通过两轮 race。迁移时的默认暂停策略、管理界面、控制变更的完整历史以及领取/签发跨事务窗口仍需继续补齐，不能把该接口视为完整协调切换已完成。

任务领取与授权签发的跨事务窗口已合并：生产 HTTP 领取统一在业务领取事务内保存加密快照、摘要和单次授权，提交成功后才返回任务；任一授权写入或提交失败均回滚领取次数和相关业务修改。生产中不再存在独立提交授权或仅领取不授权的公开 Store 入口；业务选择/授权单元测试的独立构造入口仅位于 _test.go。应用部署、Agent 更新、网关组件三类任务注入授权 INSERT 失败和延迟外键 COMMIT 失败，均保留原状态与 attempt，授权及 claimed 审计无部分残留；故障移除后的两个并发领取恰好一个成功、一个授权记录。故障注入、实际 Agent→Center 流程、暂停、更新交接、旧证据切换及 OpenAPI 通过两轮 race；移除旧生产入口后另一次定向回归通过。结果投影事务边界及其余任务族的完整故障覆盖仍需继续。
