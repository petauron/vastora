# #412 / #428 交付盘点

以当前工作区和 GitHub Issue 正文为准。此文件归纳剩余工作，取代进度日志中已过时的“尚未实现”陈述；详细测试证据仍保留在原日志。状态：未完成、未提交、未部署。发布流程优化文件属于原有工作，不能混入此次交付。

## MVP 发布范围（以用户最新决定为准）

用户要求先上线 MVP，不再持续扩展收尾和验收。本次交付保留 Center 持久授权、Agent 内存执行、失败/未知停止屏障、管理员明确放弃或重新执行、已有成功证据确认，以及本地 HTTP 客户端生命周期修复。保留前向迁移、身份、加密证据和必要备份，不保留旧执行兼容路径。

以下改为后续项，不作为 MVP 发布门槛：缺失结果的专用核验/凭据轮换界面、发布清理和探针备份的专用维护界面、完整 Agent 的长期低内存验收。缺失证据不能确认成功；需人工核对资源与凭据后明确放弃或重新执行。保留的备份和中断容器不能自动覆盖、回滚或继续，须先由操作者在维护窗口处理。

复用已有验证；本地不新增测试、构建或压力验证。提交发布采用仓库现有 alpha CI 和发布构建门槛。以下盘点保留为开发记录，不再代表首版必须追加的工作。

## 固定范围

最新验证约束：用户明确指出测试过多；不再每轮/每处改动运行整包回归，不新增长时间验收。后续集中完成代码，只保留必要编译和直接相关的最小验证；已有证据复用，未执行的完整运行验证不得宣称通过。

- #428：修复 Agent/tailscaled 本地 HTTP 连接生命周期，覆盖相关客户端释放及隔离低内存验证。不要求复现或证明生产卡死的全部原因。
- #412：Center 持久授权与证据，Agent 当前执行使用内存；出错停止，人工处置后才能推进；安全迁移旧回执/一次性结果，删除被取代机制；在低内存环境验证。
- 不新增应用功能、协议选择、自动恢复模式、基础设施组件或额外兼容层。不以这次工作顺带重做发布流程。生产变更不是本次开发验证的前提，也未获得本轮授权。
- 用户进一步明确：alpha 允许破坏性更新，已作废的接口/功能直接废弃，不为旧行为开发兼容功能。旧回执只做必要的安全转移与归档核对，不建设旧协议的完整确认、重新执行或恢复管理产品。保留已有凭据/一次性结果和必要备份，不等于保留旧执行兼容。

## 实际剩余项（固定五组）

### 1. 把已经实现的处置接到界面

普通任务 confirm-completed 与 legacy resolve-legacy 已接入 api 路由；旧回执仅提供 abandon，前端明确拒绝旧任务确认/重新执行请求。列表由后端解密验证保留的成功结果，仅输出 canConfirm 布尔值，不暴露凭据；普通任务据此显示确认动作，后端提交时仍完整验证证据和业务状态。现有确认、停止勾选、备注和错误反馈组件复用，未改版。前端三文件九测试通过；后端分页/确认定向测试已执行。

仍需补真实缺失结果及发布清理的人工处置、相关界面核对信息与集成验证。保持现有页面样式、简洁文案及危险操作确认；技术错误保留在详情，不新增管理页面设计任务。

### 2. 完成未知结果、凭据和旧回执的闭环

`ConfirmExecution` 目前只接受已保存且标记成功的结果；真正缺失/不确定的结果仍没有完整的显式核对/凭据轮换路径。不能为了能点击而直接把未知状态改成成功，也不能要求不存在的上报证据。

`AbandonLegacyReceipt` 已保留加密归档并原子终止匹配的旧业务尝试。按用户最新范围，不再开发旧记录的确认/重新执行或旧 helper 兼容流程；只补必要的切换核对和安全归档，包括记录已消失/被替代时如何不阻塞新协议。处置不重放旧命令、不覆盖新尝试，确认旧执行停止并记录操作者。

已补 archive-only 退役：旧业务记录不存在、匹配尝试已变化或类型已作废时，管理员确认旧执行停止并留下核对备注后，只处置归档；不创建兼容执行、不覆盖新业务尝试。仍保留身份校验、原子审计和原始加密凭据。测试覆盖正常、审计失败、变更尝试、缺失业务、作废类型、非管理员、未确认停止七场景。

### 3. 删除被取代机制，封住剩余自动变更路径

当前 RunTasks 已不再轮询旧回执/启动恢复。已删除 deliverTaskCompletion、未被使用的 maintainer/restorer 接口和旧 recovery scope/allowlist。旧 `task_receipts.go` 已整体删除：不再保留 Prepare/Record/Pending/Acknowledge/Resolve、自动重放/重新启用、30 天清理器或旧任务哈希逻辑；`agent resolve-legacy-task` CLI 与旧 promotion 回执恢复判定同时删除。当前结果类型仅用于内存执行；归档读取与历史密文验证独立保留，不运行旧协议。

测试直接构造历史数据库记录，不以保留旧生产写入 API 作为夹具。移除仅验证旧重放/自动重新启用/清理器的测试；保留前向迁移备份、失败关闭、原始密文不变、归档确认后退役，以及 promotion 备份重启后不自动推进的验证。其余旧恢复实现和后续执行步骤仍需按固定范围收尾，不能删除身份、已安装配置或 helper 最小保护材料。

本轮退役后的验证：Agent 的 `TestExecution|TestLegacyReceipt|TestTaskReceiptSchemaV19|TestAgentSchemaV10|TestThreeXUIControllerPromotionPreservesBackup|TestLandingServerTaskIdentity` 通过（1.708s）；Center 执行/归档定向测试通过（8.932s）；cmd/vastora 编译通过，该过滤器未匹配命令包测试。首次检查发现旧测试删除后的 platform 无用导入，删除后 Agent 定向重跑通过。`git diff --check` 通过，Go 代码和 CLI 中无已删除回执 API/命令引用。未运行全包回归，未提交、未改生产。

网关旧 restore/PrepareGatewayStartup/启动屏障状态已删除；心跳不应用旧网关配置，并发顺序测试改为当前变更之间的互斥验证。旧 Komari Docker→host 专用兼容迁移已删除。手动升级的自动回退也已改为失败停止并保留备份。

原生 Komari 的旧 Docker 清理兼容分支也已删除：安装后不再强删旧容器，清理失败不再补偿卸载原生探针；卸载主机服务失败后不继续发 Docker 删除。新增安装/卸载 × 主机成功/失败四场景，使用可观测 HTTP 服务断言零 Docker 请求且无补偿调用。`go test ./internal/agent -run 'TestApplicationExecutor.*Komari' -count=1` 通过（1.303s），`git diff --check` 通过；未改生产。

ApplicationExecutor.Restore/Maintain、containerMatchesInstallation、OfflineRestore、HostApplicationRestorer、RestoreKomari/RestorePulse 及对应恢复错误类型已删除；不再保留由已安装快照构造离线安装任务、自动启动容器或重写探针配置的入口。仅验证旧恢复语义的测试已移除，原生安装/升级/卸载和身份保留测试保留。Agent 全包回归通过（6.224s），已删除符号在 internal/cmd 中无引用，diff 检查通过。

REALITY 创建的失败删除补偿、次数阈值回滚、响应丢失后继续配置及半成品递归重建已删除。已存在同命令入站时停止并要求显式核对，不覆盖或删除。五个原有故障测试改为保留现场、不重复创建及不删除的断言，REALITY 定向测试通过（1.210s）；不是全部命令路径已完成的证据。

旧 deferred 双错误类型、次数阈值和延后/阈值转人工判定已全部删除，所有调用点统一使用 uncertainTaskOutcome；不再保留旧名称别名。不确定结果的上报不依赖 attempt，第一次就需要处置。普通错误与不确定结果区分、原因解包及高次数不回滚测试保留；Agent 全包通过（6.335s），已删除符号无引用，diff 检查通过。

探针 ApplyKomari/ApplyPulse 的失败回滚、取消后继续 stop/disable/reload/restart 已删除。变更前将已有受管文件保存在权限 0600 的 .previous 备份；已有备份未核对时拒绝覆盖；成功后清理备份，失败保留现状及 Pulse 身份。旧恢复测试断言改为保留备份/现场。按用户最新要求，本轮仅格式与 diff 检查，未重跑测试。

补充：逐文件写入、备份建立/删除、Pulse token 清理前检查取消，取消后不再进入下一步。补充取消备份/清理的最小测试代码，但本轮未运行。

另已修复部分结果丢失：容器配置已生成 API token 后的校验/晋升/清理错误保留 token，部署层继续带回 GeneratedSecrets；REALITY 部分结果在失败时进入加密结果证据，不被成功判断丢弃。相关定向验证已通过（用户减少测试要求之前）。

REALITY 重命名和订阅 host 子调用的错误后读回成功转继续逻辑已删除；写请求失败直接报告不确定，不推进后续 host/HY2 变更。重命名故障测试改为确认无错误后读回，新增 host 创建/更新两个失败场景，均断言只有一次前置读取、一次写入。Agent 全包通过（5.764s）。

发布入口清理的 RunPublicationCleanup/retryPublicationCleanups/backoff 已删除，Center 启动不再调度该循环；失败持久保留 pending/attempt/error、标记 action_required，不设置再次执行时间，调用者收到原始失败。清理函数不重复执行失败记录，Cloudflare 清理失败不推进后续资源；整机卸载观察到未完成入口清理立即停止。Publication/Decommission 定向测试通过（9.478s），删除符号无引用，diff 检查通过。失败记录的显式处置仍需接入第 1/2 组闭环，不能把删除自动重试当作完整可操作流程。

容器 replace 主流程的 abort/rollback 闭包、启动时恢复旧事务、删旧 candidate、空卷回收重建、rename 错误后读回继续、清理失败伪报成功已删除。原库快照/持久备份、所有权校验和正常健康替换保留；检测到旧 candidate/backup/cleanup 时停止要求核对。11 项旧失败恢复断言改为保留容器/卷/快照、不恢复数据库、不重启旧服务及保留未知结果。容器替换定向测试通过（0.473s），尚未进行本轮全包回归。

独立 recover/maintain、重启旧容器、回填数据库和重建标记函数已删除；部署及协议调整改为只读检查未决替换，存在残留则停止。保留数据卸载仅按顺序停止写入者，不 rename/恢复数据库/启动服务；Stop 响应失败立即退出。旧恢复专用测试退役，新增三类残留阻止执行及正常/响应丢失两类 keep-data 停止测试；正常卸载和所有权测试仍保留。Agent 全包通过（5.624s），删除符号无引用，diff 检查通过。

剩余具体代码：其它 REALITY 子调用和数据恢复工具的剩余调用需最终审计；容器故障测试文案及更严格的错误后调用序列断言也需整理。保留只读心跳/观察，不主动停止已有正常应用。

### 4. 回归状态（原记录的失败已处理）

本轮完整包验证：

- `internal/agent`：通过，6.862s。
- `internal/landing`：通过，2.029s。
- `internal/center`：通过，49.197s。
- `cmd/vastora`：本机回归通过，3.584s。依赖 Linux 构建目标的五个下载/安装测试明确跳过 macOS，不能以此证明 Linux 更新成功。

随后在获授权的非生产 Linux 隔离容器中实际执行更新下载、永久错误、取消、候选版本校验、备份保留及重启失败停止测试，全部通过。容器限制 256 MiB、1 CPU、128 PID、无网络、只读根目录、无宿主机服务 socket；仅测试临时目录允许执行候选程序。首次因 tmpfs 默认 noexec 导致版本检查无法启动，读取挂载参数证实后只修正隔离环境，未放宽版本/摘要校验。restart 使用测试回调，没有启动或重启宿主机服务；这不是完整 Agent 资源验证。

处理内容：

- Agent helper 夹具提供新授权；重启后 Center 拒绝旧授权时不再次调度。调度成功不提前上报主机任务完成，调度失败只报告一次。
- 取消发生后只报告不确定结果，不再持久化应用成功；租约失败取消当前执行。保留网关各类变更的互斥/顺序验证，移除旧离线恢复预期。
- CPA 结果夹具补齐当前签名清单的两个服务；历史数据库夹具使用实际迁移序列和未绑定密钥状态，保留错误密钥失败关闭检查。
- 失败卸载 helper 必须先显式人工重新执行，才产生新尝试和回调凭据。
- 删掉旧 Komari Docker→host 专用迁移及旧目录读取，运行时调整保留已安装清单；站点夹具使用稳定站点 ID 和各站点自己的网关；时钟夹具与目录和登录限流一致。
- 手动 `agent update` 发现真实遗漏：失败会自动降级并第二次重启。现改为先原子复制旧二进制备份，再原子替换已验证候选；错误/取消停止，重启失败保留候选和备份供显式维护，不自动回退。
- 下载取消测试的辅助 goroutine 在失败退出时完整收尾，避免测试死锁掩盖原始失败。

这证明当前相关包回归及上述 Linux 更新路径通过，不代表第 1、2、3、5 组已全部完成。

### 5. 完成原 Issue 要求的隔离运行验证

已有 256/512 MiB 测试函数级别记录及短期 socket/FD/goroutine 有界证据；还不是完整 Agent 正常频率下的空闲、正常任务、Center 故障验证。

使用用户授权的非生产 Linux 环境，独立限额、无宿主机业务 socket，记录 RSS/PSS、FD/Unix socket、goroutine、cgroup I/O、CPU/唤醒、PSI/refault。保留正常、错误、取消、替换场景，不进行生产压力测试。不增加必须证明某台生产故障唯一根因的验收。

## 完成判定

以上五组完成后，对照两个 Issue 的原始条款作一次交付核对。不继续追加新的功能性验收。代码、测试、提交、发布和线上生效分别报告；当前不具备“全部完成/可直接发布”的证据。
